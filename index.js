// index.js
const { double, square, isEven, multiply, isOdd } = require("./math");
// Run this file to see results of math functions in console

// Assuming all functions are defined in the same file (math.js) for simplicity
// If you're using modules, you'd import them here instead

// Function calls

console.log("double(4) =>", double(4));
console.log("square(5) =>", square(5));
console.log("isEven(6) =>", isEven(6));
console.log("multiply(3, 7) =>", multiply(3, 7));
console.log("isOdd(9) =>", isOdd(9));
